# Dependencies
node_modules/

# Runtime data (cache, EPG DBs – no persistence by design)
data/

# Temp files
temp/
tmp/
*.log

# Local config (can contain private URLs)
addon-config.json

# Environment and secrets
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# IDE / editor
.idea/
.vscode/
*.swp
*.swo

# Docker: ignore local override only (Dockerfile and docker-install docs are committed)
# docker-compose.override.yml
# .dockerignore is committed (used at build time)
